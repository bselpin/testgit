import style from "styles/components/detail.module.scss"

const OperationLtc = () => (
	<div className={style.detail_box}>
		<p className={style.title}>운영 정보</p>
		<div className={style.detail_info_box}></div>
	</div>
)

export default OperationLtc
